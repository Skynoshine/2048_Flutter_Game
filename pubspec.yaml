name: game
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  build_runner: ^2.5.4
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  json_annotation: ^4.9.0
  json_serializable: ^6.9.5
  logger: ^2.6.0
  path_provider: ^2.1.5
  provider: ^6.1.5
  sembast: ^3.8.5+1
  unique_names_generator: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
